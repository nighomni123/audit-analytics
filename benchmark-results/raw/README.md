# Raw benchmark evidence

This directory intentionally contains no client-like datasets. The harness writes only compact command output, hashes, and measurements to the parent JSON artifacts. Temporary generated CSV/database/evidence files are created under the operating system temporary directory and deleted after each phase.
